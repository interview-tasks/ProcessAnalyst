# name: Deploy Dashboard to GitHub Pages

# on:
#   push:
#     branches: [ main ]
  
#   # Allow manual deployment
#   workflow_dispatch:

# # Sets permissions of the GITHUB_TOKEN to allow deployment to GitHub Pages
# permissions:
#   contents: read
#   pages: write
#   id-token: write

# # Allow only one concurrent deployment
# concurrency:
#   group: "pages"
#   cancel-in-progress: false

# jobs:
#   # Build job
#   build:
#     runs-on: ubuntu-latest
#     steps:
#       - name: Checkout
#         uses: actions/checkout@v3
        
#       - name: Setup Pages
#         uses: actions/configure-pages@v3
      
#       # Create .nojekyll file in root
#       - name: Create .nojekyll file
#         run: touch .nojekyll
      
#       # Add a .nojekyll file to dashboard directory just to be safe
#       - name: Create dashboard .nojekyll file
#         run: |
#           if [ ! -f dashboard/.nojekyll ]; then
#             touch dashboard/.nojekyll
#           fi
      
#       # Copy the updated dashboard.js file to the right location if it's not in the repository
#       - name: Ensure dashboard.js is updated
#         run: |
#           if [ -f "dashboard/assets/js/dashboard.js" ]; then
#             echo "Checking if dashboard.js needs to be updated with correct paths..."
#             if ! grep -q "dataPath: 'dashboard/data/'" dashboard/assets/js/dashboard.js; then
#               echo "Updating dashboard.js with correct paths..."
#               sed -i 's/dataPath: .data\//dataPath: "dashboard\/data\/"/g' dashboard/assets/js/dashboard.js
#               sed -i 's/chartsPath: .charts\//chartsPath: "dashboard\/charts\/"/g' dashboard/assets/js/dashboard.js
#             else
#               echo "dashboard.js already has correct paths"
#             fi
#           else
#             echo "dashboard.js not found - this might cause issues"
#           fi
      
#       # Debug - list key files to verify structure
#       - name: Check important files
#         run: |
#           echo "Root directory index.html:"
#           ls -la index.html || echo "No index.html in root directory"
          
#           echo "Dashboard directory index.html:"
#           ls -la dashboard/index.html || echo "No index.html in dashboard directory"
          
#           echo "Dashboard JavaScript file:"
#           ls -la dashboard/assets/js/dashboard.js || echo "No dashboard.js file found"
          
#           echo "Dashboard CSS file:"
#           ls -la dashboard/assets/css/dashboard.css || echo "No dashboard.css file found"
          
#           echo "First few lines of root index.html:"
#           head -20 index.html || echo "Cannot display index.html content"
      
#       - name: Upload artifact
#         uses: actions/upload-pages-artifact@v1
#         with:
#           path: './'
  
#   # Deployment job
#   deploy:
#     environment:
#       name: github-pages
#       url: ${{ steps.deployment.outputs.page_url }}
#     runs-on: ubuntu-latest
#     needs: build
#     steps:
#       - name: Deploy to GitHub Pages
#         id: deployment
#         uses: actions/deploy-pages@v1